<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.group3p1.healthyEye">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".presentation.ui.exercises.AddExerciseActivity"></activity>
        <activity android:name=".presentation.ui.exercises.detail.ExerciseDetail" />
        <activity android:name=".presentation.ui.exercises.list.ExerciseList" />
        <activity android:name=".presentation.ui.main.ExercisesActivity" />
        <activity android:name=".presentation.ui.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>